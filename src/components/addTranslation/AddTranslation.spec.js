import { mount } from 'enzyme';
import AddTranslation from './AddTranslation';

describe('My Test Suite', () => {
    it('My Test Case', () => {
        const comp = mount(<AddTranslation />)
        expect(true).toEqual(true);
    });
});